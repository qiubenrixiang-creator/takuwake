# セットアップ手順（PC作業用）

卓分けの館を、新しいGitHubリポジトリで公開するまでの手順です。
**GitHubだけで完結します。** Firebaseも、Vercelも、他のサービスも要りません。

上から順にやれば終わります。所要20分ほど。

---

## 0. 準備するもの

- このフォルダ（`takuwake`）一式
- GitHubアカウント
- GitHub Desktop … 未インストールなら `desktop.github.com` から入れてください

以上です。

---

## 1. 古いリポジトリを削除する

新しく作り直すので、先に古い方を消します。
同じ名前を使うため、これを先にやらないと名前が取れません。

1. GitHubで今の `takuwake` リポジトリを開く
2. 上の「**Settings**」タブ
3. ページ最下部までスクロール →「**Danger Zone**」
4. 「**Delete this repository**」
5. 確認欄にリポジトリ名を入力して実行

> 別の名前で作るなら、この手順は飛ばして構いません。
> ただし公開URLが変わるので、以前のURLを誰かに伝えてある場合は注意してください。

---

## 2. 新しいリポジトリを作る

1. GitHub右上の「**+**」→「**New repository**」
2. 次のとおり入力します

   | 項目 | 設定 |
   |---|---|
   | Repository name | `takuwake` |
   | Description | ボードゲーム会向けの席決めツール（任意） |
   | 公開設定 | **Public**（GitHub Pages の無料版に必要） |
   | Add a README file | **チェックを入れる** |
   | .gitignore / license | どちらも None |

3. 「**Create repository**」

> 公開すると、音源ファイルは誰でもダウンロードでき、隠しコマンドや秘宝の入手条件も
> ソースから読める状態になります。身内で遊ぶぶんには問題ありません。
> 使っている音源素材の規約に「再配布禁止」がないかだけ、一度確認しておいてください。

---

## 3. PCに取り込む

1. GitHub Desktop を開く →「File」→「**Clone repository**」
2. 「GitHub.com」タブに `takuwake` があるので選ぶ
3. **Local path** を覚えておく（例：`C:\Users\〇〇\Documents\GitHub\takuwake`）
4. 「**Clone**」

---

## 4. ファイルを入れる

1. GitHub Desktop の「Repository」→「**Show in Explorer**」
2. 中にある `README.md` を削除します（このフォルダのものに差し替わります）
3. このフォルダの**中身**を、そこへコピーして貼り付け

   ```
   assets   css   js              ← フォルダ3つ
   index.html   kumi.html   README.md   SETUP.md   .gitignore
   ```

> **`.git` という隠しフォルダがあれば、絶対に消さないでください。**
> 表示されていなければ気にしなくて大丈夫です。

---

## 5. GitHubへ送る

1. GitHub Desktop に戻ります
2. 左側の変更ファイル一覧に、`assets/audio/bgm_main.mp3` のように
   **スラッシュ付きのパス**が並んでいることを確認してください

   > `bgm_main.mp3` のようにスラッシュ無しで並んでいたら、
   > フォルダが潰れています。手順4をやり直してください。

3. 左下の「Summary」に `初回アップロード` と入力
4. 「**Commit to main**」
5. 上部に出る「**Push origin**」

---

## 6. 公開設定（GitHub Pages）

1. リポジトリの「**Settings**」→ 左メニュー「**Pages**」
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / `(root)` を選んで「**Save**」
4. 数分待つと、上部にURLが表示されます

**これで完成です。** 設定はここで終わりです。

---

## 7. 動作確認

公開URLを開いて、次の順に確かめてください。

- [ ] 画面が黒地のドット絵で表示される（真っ白なら手順4か5の失敗）
- [ ] 「音声をありで始める」で音が鳴る
- [ ] 診断を最後まで進めると、**4文字の合言葉**が出る
- [ ] 名前の入力欄に `たくぐみ` と打つと卓組みの間へ入れる
- [ ] 卓組みの間で、名前と合言葉を入れて「迎える」→ 2人以上で「卓を組む」
- [ ] 「発表リンクを作る」でリンクがコピーできる
- [ ] そのリンクを別のブラウザで開き、「📜 記録」に席の一覧が出る

スマホでも一度確認してください。特に音は、iPhoneとAndroidで挙動が違います。

---

## 当日の流れ

1. 参加者に公開URLを教える（QRにしておくと楽です）
2. 各自が診断すると、画面に**4文字の合言葉**が出る
3. 幹事は `たくぐみ` で卓組みの間に入り、**名前と合言葉**を打ち込んでいく
4. 全員そろったら「**卓を組む**」
5. 気になるところは名前をたたいて入れ替える
6. 「**発表リンクを作る**」→ LINEなどに貼る
7. 参加者はリンクを開けば、以後いつでも「📜 記録」から席を確認できる

リンクを配らない場合は、「卓に置く札を印刷する」で各卓に紙を置くだけでも足ります。

---

## 幹事の合言葉

名前の入力欄に打ちます。**参加者には教えないでください。**

| 合言葉 | はたらき |
|---|---|
| `たくぐみ` | 秘宝と鍵を解放して、卓組みの間へ渡る |
| `かいほう` | 図鑑（卓・称号・秘宝）をその場で全解放 |
| `かたりべ` | 継承の儀を開く |

合言葉も、表示される文章も `js/data.js` の先頭にある `ADMIN_WORDS` で変えられます。

卓組みの間は `kumi.html` です。URLを直接開いても錠がかかっていて、合言葉を求められます。

---

## これ以降ファイルを直すとき

1. PCのフォルダでファイルを直す
2. GitHub Desktop で Summary を書いて「Commit to main」
3. 「Push origin」

数分で公開URLに反映されます。
文章や質問だけを直したいときは `js/data.js` だけを触ってください。

---

## 困ったとき

**画面が真っ白／文字だけが縦に並ぶ**
`css` と `js` フォルダが正しい場所にありません。手順4からやり直してください。

**音が鳴らない**
`assets/audio` フォルダが入っているか確認してください。
それでも鳴らない場合は、iPhone本体左側の消音スイッチを確認してください。

**合言葉が「読み取れぬ」と言われる**
4文字すべてが合っているか確認してください。1文字でも違うと弾かれる作りです。
`0`と`O`、`1`と`I` は使っていないので、見間違いなら読み替えてみてください。

**卓組みの間に入れない**
`kumi.html` がリポジトリ直下にあるか確認してください。

**名簿が消えた**
卓組みの名簿は幹事の端末の中だけにあります。
別の端末やシークレットウィンドウで開くと空になります。
当日はずっと同じ端末で操作してください。
